import ReactDOM from 'react-dom';
import {App} from './App';

//document.getElementById("root").appendChild(App());

//react 안의 dom이 App 컴포넌트를 렌더링
ReactDOM.render(App(), document.getElementById('root'));